# we can also make a package called trianglepack for example
# we will copy paste the function in the __init__.py script

import trianglepackage

size = int(input())
trianglepackage.print_triangle(size)

# note - a package or a module cannot have numbers, - etc in its name